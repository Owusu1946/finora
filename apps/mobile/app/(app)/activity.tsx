import { ScreenStub } from "@/components/shell/screen-stub";

export default function ActivityScreen() {
  return (
    <ScreenStub
      title="Activity"
      description="Searchable transaction history will appear here once WeWire is connected."
    />
  );
}
