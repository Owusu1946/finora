import { ScreenStub } from "@/components/shell/screen-stub";

export default function ContactsScreen() {
  return (
    <ScreenStub
      title="Contacts"
      description="AI-managed recipients and beneficiaries. Pay people by name from chat."
    />
  );
}
