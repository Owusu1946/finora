import { ScreenStub } from '@/components/shell/screen-stub';

export default function SettingsScreen() {
  return (
    <ScreenStub
      title='Settings'
      description='Approvals, security, and account preferences will be configured here.'
    />
  );
}
