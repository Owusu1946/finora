EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_replace_me
# Required for production builds. Expo development derives the host from Metro automatically.
EXPO_PUBLIC_API_URL=https://api.example.com
# Opt in to the authenticated OpenAI gateway. The local mock runtime remains the default.
EXPO_PUBLIC_REMOTE_CHAT_ENABLED=false
