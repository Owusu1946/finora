# @finora/mobile

Conversation-first Finora app. Chat UI

## Stack

- Expo Router drawer shell
- `@assistant-ui/react-native` Thread / Composer / Message / ThreadList
- Zinc palette + StyleSheet (with-expo tokens)
- Local mock chat adapter (swap for Finora API later)

## Run

```bash
# from repo root
bun run mobile
```
